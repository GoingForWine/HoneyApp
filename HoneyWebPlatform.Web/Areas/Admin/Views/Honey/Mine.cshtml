@model MyHoneysViewModel

@{
    ViewBag.Title = "Моите Медове";
}

@*<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<h4>Моите добавени Медове</h4>*@

@if (!Model.AddedHoneys.Any())
{
    <h2 class="text-center">Все още нямате добавени Медове!</h2>
}

@*<div class="row">
    @foreach (var honey in Model.AddedHoneys)
    {
        @await Html.PartialAsync("~/Views/Shared/_HoneyPartial.cshtml", honey)
    }
</div>*@


<section class="breadcrumb breadcrumb_bg">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb_iner text-center">
                    <div class="breadcrumb_iner_item">
                        <h2>Моите Медове</h2>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- breadcrumb start-->
<!--::chefs_part start::-->
<!-- food_menu start-->
<section class="food_menu gray_bg">
    <div class="container">
        <div class="row justify-content-between">
            <div class="col-lg-5">
                <div class="section_tittle">
                    <p>Моят Домашно произведен</p>
                    <h2>Мед</h2>
                </div>
            </div>
            <div class="col-lg-6">
            </div>
            <div class="col-lg-12">
                <div class="tab-content" id="myTabContent">
                    <div class="tab-pane fade show active single-member">
                        <div class="row">
                            <div class="col-sm-6 col-lg-6">
                                @foreach (var honey in Model.AddedHoneys)
                                {
                                    @await Html.PartialAsync("~/Views/Shared/_HoneyPartial.cshtml", honey)
                                }
                            </div>
                            @*<div class="col-sm-6 col-lg-6">

                                </div>*@
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>